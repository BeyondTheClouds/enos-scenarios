---
  KeystoneBasic.create_delete_user:
    -
      args: {}
      runner:
        type: "constant"
        times: {{times}}
        concurrency: {{concurrency}}
      sla:
        failure_rate:
          max: 0

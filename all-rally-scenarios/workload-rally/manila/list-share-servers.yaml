---
  ManilaShares.list_share_servers:
    -
      args:
        search_opts: {}
      runner:
        type: "constant"
        times: {{times}}
        concurrency: {{concurrency}}
      sla:
        failure_rate:
          max: 0
